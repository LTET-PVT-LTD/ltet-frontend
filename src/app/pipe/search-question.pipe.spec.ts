import { SearchQuestionPipe } from './search-question.pipe';

describe('SearchQuestionPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchQuestionPipe();
    expect(pipe).toBeTruthy();
  });
});
