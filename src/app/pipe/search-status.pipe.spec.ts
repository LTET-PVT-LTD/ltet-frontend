import { SearchStatusPipe } from './search-status.pipe';

describe('SearchStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
